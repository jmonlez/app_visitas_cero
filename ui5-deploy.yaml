specVersion: "4.0"
metadata:
  name: visitas.cero.visitas0
type: application
resources:
  configuration:
    propertiesFileSourceEncoding: UTF-8
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateCachebusterInfo
      configuration:
        archiveName: visitascerovisitas0
        relativePaths: true
        additionalFiles:
          - xs-app.json
